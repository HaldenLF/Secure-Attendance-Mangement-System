<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    tools:context=".ui.login.LoginActivity">
    <EditText
        android:id="@+id/SigninUsername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Username"
        android:height="48dp"
        android:layout_marginTop="50dp"/>
    <EditText
        android:id="@+id/SigninEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Email"
        android:height="48dp"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/SigninUsername"/>
    <EditText
        android:id="@+id/SigninPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:height="48dp"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/SigninEmail"/>
    <TextView
        android:id="@+id/fingerprintSignInText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign in with fingerprint below"
        android:textSize="14sp"
        android:layout_marginTop="50dp"
        android:layout_below="@id/signinBtn"/>
    <Button
        android:id="@+id/signinBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/SigninPassword"
        android:layout_marginTop="50dp"
        android:text="Sign in" />
    <Button
        android:id="@+id/FPScanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Use Finger Print"
        android:layout_marginTop="15dp"
        android:layout_below="@+id/fingerprintSignInText" />
</RelativeLayout>